#include "pid_controller.h"
#include "esphome/core/log.h"

namespace esphome {
namespace pid_controller {

// (No extra implementation needed if everything is inline in the header.)

}  // namespace pid_controller
}  // namespace esphome